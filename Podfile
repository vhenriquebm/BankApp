# platform :ios, 'A versão mínima do seu app principal (ex: 14.0)'

target 'BankApp' do
  # Outras dependências do seu app principal aqui

  pod 'TransferModule', :git => 'https://github.com/vhenriquebm/TransferModule.git'
end
